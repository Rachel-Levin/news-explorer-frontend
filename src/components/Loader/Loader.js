import './Loader.css';

function Loader() {
    return (
        <section className='loader'>
            <i className='loader_animation'></i>
            <p className='loader_text'>Searching for news...</p>
        
        </section>
    );
};

export default Loader;